spring:
  application:
    name: location-updator
    group: 
    version:  
  datasource:
    url: jdbc:mysql://localhost:3306/dev_location_prod
    username: root
    password: Test@12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8082
  servlet:
    context-path: /location-updator